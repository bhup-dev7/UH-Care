{
  "name": "uh-care",
  "version": "1.0.0",
  "description": "UH Care - Healthcare Management System for United Home Care",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["healthcare", "management", "hospital", "patient", "appointments"],
  "author": "UH Care Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
